# Per-run secrets, generated by run.sh. Never committed: a compose file with a
# working secret in it is a secret that ends up in somebody's deployment.
.env

# Container logs, Playwright traces and screenshots from the last run.
artifacts/

node_modules/
